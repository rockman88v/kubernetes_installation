ingress:
  ## If true, Kubernetes Dashboard Ingress will be created.
  ##
  enabled: true
  ## Kubernetes Dashboard Ingress Class
  # className: "example-lb"
  className: local
  paths:
    - /
  hosts:
    - kubernetes-dashboard.prod.viettq.com